#define CATCH_CONFIG_MAIN  // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.hpp"

TEST_CASE("Dummy test", "[A]") {
	//I'm here until we start the proj for real
	REQUIRE(1);
}